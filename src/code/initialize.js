const net = require('net')

class Initialize {
    constructor(value) {
        function start() {
        
        }

        switch(value) {
            case 0:
                start()
            break
        }
    }
}

module.exports = Initialize